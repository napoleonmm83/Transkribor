{
  "test": "python -m pytest webtool/test_jobs.py -q",
  "pfade": [
    "webtool/jobs.py",
    "webtool/test_jobs.py"
  ],
  "mutationen": [
    {
      "id": "M1 die monotone Buchung in remove_base faellt weg",
      "von": "                r.setdefault(\"entfernt_je\", set()).update(aliases or {base})",
      "nach": "                # M1: entfernt_je wird nie gebucht — die Menge bleibt leer",
      "rot": [
        "test_remove_base_bucht_entfernt_bedingungslos_und_get_liefert_eine_kopie",
        "test_reannoncement_reaktiviert_die_server_menge_deckelfest"
      ],
      "gruen": [
        "test_jobs_start_mit_initialem_base_scope"
      ],
      "datei": "webtool/jobs.py"
    },
    {
      "id": "M2 der [active]-Lift in buche_aktive faellt weg (Sperre traegt ins Zweitleben)",
      "von": "        if entfernt_je is not None and roh:\n            _entferne_base_aliases(entfernt_je, roh)",
      "nach": "        # M2: keine neue [active]-Zeile hebt die monotone Loeschung mehr auf",
      "rot": [
        "test_reannoncement_reaktiviert_die_server_menge_deckelfest"
      ],
      "gruen": [
        "test_remove_base_bucht_entfernt_bedingungslos_und_get_liefert_eine_kopie"
      ],
      "datei": "webtool/jobs.py"
    },
    {
      "id": "M6 die Alias-Loeschung entfernt nur eine Schreibweise",
      "von": "def _entferne_base_aliases(namen: set[str], gesucht: str) -> None:\n    \"\"\"Entfernt alle Schreibweisen derselben Dateisystem-Base.\"\"\"\n    aliases = {name for name in namen if _gleiche_base(name, gesucht)}\n    namen.difference_update(aliases)",
      "nach": "def _entferne_base_aliases(namen: set[str], gesucht: str) -> None:\n    \"\"\"M6: Entfernt nur einen beliebigen Alias.\"\"\"\n    alias = _base_schluessel(namen, gesucht)\n    if alias is not None:\n        namen.discard(alias)",
      "rot": [
        "test_entferne_base_aliases_loescht_alle_gleichen_schreibweisen_portabel"
      ],
      "gruen": [
        "test_reannoncement_reaktiviert_die_server_menge_deckelfest"
      ],
      "datei": "webtool/jobs.py"
    },
    {
      "id": "M3 das vierte Argument am buche_aktive-Aufruf faellt weg (Lift laeuft leer)",
      "von": "                    buche_aktive(_jobs[jid][\"active_bases\"], line, zulassung,\n                                 je_sperre)",
      "nach": "                    buche_aktive(_jobs[jid][\"active_bases\"], line, zulassung)  # M3",
      "rot": [
        "test_reannoncement_reaktiviert_die_server_menge_deckelfest"
      ],
      "gruen": [
        "test_remove_base_bucht_entfernt_bedingungslos_und_get_liefert_eine_kopie"
      ],
      "datei": "webtool/jobs.py"
    },
    {
      "id": "M5 der Art-Filter am Lift faellt weg (fremder fetch-Text hebt die Sperre)",
      "von": "            je_sperre = (_jobs[jid].get(\"entfernt_je\")\n                         if _jobs[jid][\"kind\"] in NACHTRAG_KINDS else None)",
      "nach": "            je_sperre = _jobs[jid].get(\"entfernt_je\")  # M5: jede Art hebt, auch fetch-Fremdtext",
      "rot": [
        "test_fremder_active_text_hebt_die_monotone_sperre_nicht"
      ],
      "gruen": [
        "test_reannoncement_reaktiviert_die_server_menge_deckelfest"
      ],
      "datei": "webtool/jobs.py"
    },
    {
      "id": "M4 die Auslieferung in get() faellt weg (entfernt_je bleibt ein Set im Snap)",
      "von": "        if isinstance(snap.get(\"entfernt_je\"), set):\n            snap[\"entfernt_je\"] = sorted(snap[\"entfernt_je\"])",
      "nach": "        # M4: entfernt_je reist als lebendes Set statt als sortierte Liste",
      "rot": [
        "test_remove_base_bucht_entfernt_bedingungslos_und_get_liefert_eine_kopie",
        "test_reannoncement_reaktiviert_die_server_menge_deckelfest"
      ],
      "gruen": [
        "test_jobs_start_mit_initialem_base_scope"
      ],
      "datei": "webtool/jobs.py"
    }
  ]
}
