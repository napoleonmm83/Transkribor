# Python-Abhaengigkeiten OHNE torch — das kommt aus dem CUDA-Index und wird vom Installer
# (electron/setup.js, Schritt "torch") separat installiert:
#   pip install torch --index-url https://download.pytorch.org/whl/cu128
# Beides in eine Datei zu packen ginge nur mit --extra-index-url, und dann zieht pip je nach
# Aufloesung das CPU-Rad von PyPI — die GPU waere still weg.
faster-whisper
# Nur fuer den GGML-Modell-Download in webtool/whispercpp.py: python.org-Python nutzt
# auf macOS nicht die System-Keychain, urllib scheitert dort sonst mit
# CERTIFICATE_VERIFY_FAILED. Kaeme transitiv ohnehin herein — worauf man sich
# verlaesst, gehoert benannt.
certifi
fastapi
uvicorn[standard]
python-multipart
pyannote.audio==4.0.7
# Untergrenze wegen `js_runtimes` in webtool/fetch.py (#162): eine aeltere Fassung kennt die
# Option nicht und verwirft sie STILL (params ist ein blosses dict) — der Fix waere weg, ohne
# dass irgendetwas davon berichtet. 2026.7.4 ist die Fassung, gegen die gemessen wurde.
#
# `[default]` zieht `yt-dlp-ejs` mit — die Loeserskripte fuer YouTubes JS-Challenge (#170).
# OHNE sie war die JS-Laufzeit aus #162 ueberall wirkungslos, auch im Entwickler-Checkout:
# `jsc/_builtin/ejs.py:_iter_script_sources` fragt PyPI-Paket -> Cache -> mitgeliefert ->
# GitHub, und die letzte Quelle ist per Default GESPERRT (`remote_components` ist leer).
# Mitgeliefert ist `yt.solver.core.js`, aber `yt.solver.lib.js` — das Skript, das die
# node-Laufzeit braucht — gar nicht; die Deno-Fassung im Paket ist ein 245-Zeichen-Stub, der
# npm nachlaedt. Der Loeser hat damit offline KEINE Quelle und lehnt ab; dass Downloads
# trotzdem meist klappen, liegt am Ausweichen auf Player-Clients ohne Signatur.
#
# **Das Extra statt einer eigenen Zeile**, weil die Fassung EXAKT zu `vendor/_info.py:VERSION`
# passen muss (yt-dlp prueft Major+Minor UND den Hash und verwirft sonst — mit einer Warnung,
# die `no_warnings` schluckt). yt-dlp deklariert `yt-dlp-ejs==0.8.0` und zieht den Pin mit
# jeder Fassung nach; ein freies `yt-dlp-ejs` daneben driftet frueher oder spaeter davon.
# `[pin]` waere die falsche Wahl: es nagelt auch certifi/requests/urllib3 fest und kollidiert
# damit mit dem uebrigen Baum. Neu in eine frische venv kommen mutagen, pycryptodomex UND
# brotli — letzteres verlangt im ganzen Baum kein Paket unbedingt, es haengt ueberall nur an
# einem Extra (geprueft ueber `importlib.metadata`, Zeilen ohne `extra ==`-Marker).
yt-dlp[default]>=2026.7.4
# Fuer `webtool/ytdlp_update.py:_gilt_fuer_uns` (#187): die Umgebungsmarker der yt-dlp-Metadaten
# werden AUSGEWERTET statt mit einer Zeichenkette verglichen. Kaeme transitiv ueber onnxruntime
# (pyannote-Kette) ohnehin herein — worauf man sich verlaesst, gehoert benannt; dieselbe Regel wie
# bei certifi oben. Der Import ist trotzdem abgesichert: das Modul darf nirgends werfen (#185).
packaging
# Untergrenze wegen CVE-2023-52323 (Seitenkanal im OCB-Modus, behoben in 3.19.1).
# pycryptodomex kommt neu ueber `[default]` herein und wird von yt-dlp ohne jede Grenze
# deklariert — ein Pin ist das hier NICHT (die fuehrt yt-dlp, siehe oben), nur ein Boden.
pycryptodomex>=3.19.1
